# Core dependencies for training DeepSeek-R1 models
torch>=2.1.0
transformers>=4.38.0
datasets>=2.16.0
accelerate>=0.26.0
peft>=0.8.0
bitsandbytes>=0.42.0
trl>=0.7.10
wandb>=0.16.0
sentencepiece>=0.1.99
protobuf>=3.20.0
einops>=0.7.0

# Optional: For Unsloth optimization (much faster training)
# pip install "unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git"

# Optional: For evaluation metrics
scikit-learn>=1.3.0
rouge-score>=0.1.2
nltk>=3.8.0

# Dependencies for category analysis script
openai>=1.0.0
python-dotenv>=1.0.0